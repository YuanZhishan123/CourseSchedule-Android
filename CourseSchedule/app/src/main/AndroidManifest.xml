<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 读取存储权限 (读取用户选择的 PDF 文件) -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- 课程通知：开机自启动恢复定时检查 -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- 课程通知：Android 13+ 通知权限 -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- 课程通知：Android 12+ 精准闹钟权限 -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <application
        android:name=".CStdApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher_new"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.CourseSchedule">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 课程通知：定时唤醒接收器 -->
        <receiver
            android:name=".notify.CourseAlarmReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.cstimetable.notify.CHECK_COURSE" />
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- 课程表小部件 Provider (2×2) — 标准 Android Widget -->
        <receiver
            android:name=".widget.CourseWidgetProvider"
            android:exported="true"
            android:label="@string/widget_course_name">

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/course_widget_info" />

            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
